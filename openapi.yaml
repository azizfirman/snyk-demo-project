openapi: 3.0.0
info:
  title: Demo API Snyk
  version: 1.0.0

paths:
  /user:
    get:
      summary: Get user
      parameters:
        - in: query
          name: user
          schema:
            type: string
      responses:
        "200":
          description: OK

  /admin:
    get:
      summary: Admin endpoint
      responses:
        "200":
          description: OK
